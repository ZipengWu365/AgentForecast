# Vibe coding guide

The fastest path is:

```bash
agentforecast vibe sales
```

or in Python:

```python
from agentforecast import vibe
result = vibe("sales", outdir="demo")
```

That is the intended “camera mode” mental model:

- point
- shoot
- get a pack
