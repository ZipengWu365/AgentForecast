# Benchmarks

`agentforecast` ships two benchmark surfaces:

- `generated/transparent_public_benchmark.csv`: small transparent benchmark over bundled datasets
- `generated/external_benchmark_links.csv`: links to broader public benchmarks and leaderboards

Rebuild them with:

```bash
python scripts/build_benchmarks.py
```
